internalDatabase:
  enabled: false

externalDatabase:
  enabled: true
  type: postgresql
  host: acid-minimal-cluster.default.svc.cluster.local  # This depends on your operator's service naming
  user: nextcloud
  database: nextcloud


# Nextcloud configuration
nextcloud:
  host: nextcloud.ahihi.io.vn
  username: admin
  password: "changeme"  

service:
  type: LoadBalancer

persistence: 
  enabled: true